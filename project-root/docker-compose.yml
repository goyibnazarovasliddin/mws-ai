# docker-compose: load local infrastructure

# Task:
# - Start `api` service, `db` (postgres or redis), `worker` and `redis` together.
# - Convenient for local development: `docker-compose up --build`.

# Note:
# - If using Celery, we add redis and worker services to compose.

# not used yet