/*
Nginx konfiguratsiya fayli (k8s yoki lokal reverse-proxy uchun).

Vazifasi:
- API serverga keluvchi HTTP sorovlarni yonaltirish,
- TLS/HTTP headerlarni sozlash va statik fayllarni yetkazish,
- Zarur bolsa rate-limiting yoki request buffering qoshish.

Boglanish:
- `api` servisi (uvicorn/fastapi) bilan orqa tomonda ishlaydi.
- Kubernetes ingress konfiguratsiyasi bilan birgalikda ishlatiladi.

Etibor:
- Production uchun TLS va xavfsizlik xususiyatlarini toliq sozlang.
- Local dev uchun oddiy reverse-proxy sifatida ishlatish mumkin.
*/