# Dockerfile: For building API image

# Task:
# - Creates Python environment, installs dependencies and starts `uvicorn src.app.main:app`.

# Note:
# - Build caching and multi-stage build optimizations can be added for production.

# not used yet